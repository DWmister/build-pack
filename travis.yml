language: node-js

sudo: false

cache:
  apt: true
  directries:
    - node_modules

node-js: stable

install:
  - npm install -D
  - cd ./test/smoke/template
  - npm install -D
  - cd ../../..

scripts:
  - npm test
