language: node_js

node_js: stable

install:
  - npm install -D
  - cd ./test/smoke/template
  - npm install -D
  - cd ../../..

# deploy:
#   - provider: npm
#     edge: true
#     api_token: '$NPM_TOKEN'
#     email: 17602134327@163.com
#     access: public
#     cleanup: true
#     on:
#       all_branches: true

jobs:
  include:
    - stage: deploy
      script:
        - npm version patch

      deploy:
        provider: npm
        email: $NPM_AUTH
        api_token: $NPM_TOKEN
        access: public
        cleanup: true
        edge: true
        on:
          all_branches: true
